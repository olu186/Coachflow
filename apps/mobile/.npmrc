# Skip postinstall scripts during install (e.g. react-native-screens "bob" build).
# Fixes EAS Build "Install dependencies" phase failure and local install when "bob" is not available.
ignore-scripts=true
